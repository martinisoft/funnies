.form-wrap
  %h2 Update your account
  = simple_form_for resource, as: resource_name, url: registration_path(resource_name) do |f|
    = f.input :email
    = f.input :password
    = f.hint "Leave this blank if you don't want to change it"
    = f.input :password_confirmation
    = f.input :current_password
    = f.hint "We need your current password to confirm your changes"
    %p= f.submit "Update your account"
  %h2 Cancel my account
  %p
    Unhappy? #{link_to "Cancel your account", registration_path(resource_name), confirm: "Are you sure?", method: :delete}.
